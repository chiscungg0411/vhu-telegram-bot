{
  "name": "vhu-telegram-bot",
  "version": "1.0.0",
  "main": "bot.js",
  "scripts": {
    "start": "mkdir -p ./cache && node bot.js",
    "postinstall": "echo 'Build completed successfully' || exit 1"
  },
  "dependencies": {
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "axios": "^1.6.0",
    "node-telegram-bot-api": "^0.65.0",
    "puppeteer-core": "^22.12.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
